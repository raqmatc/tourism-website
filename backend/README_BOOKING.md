# 🏨 Paygo Booking.com Backend API

Backend متكامل للاتصال بـ Booking.com API عبر RapidAPI

## 🚀 المميزات

- ✅ البحث عن المواقع والمدن
- ✅ البحث عن مئات الفنادق في الوقت الفعلي
- ✅ الحصول على تفاصيل الفندق الكاملة
- ✅ الحصول على مراجعات النزلاء
- ✅ دعم اللغة العربية
- ✅ دعم العملة السعودية (SAR)
- ✅ CORS مفعّل للاتصال من المتصفح

## 📋 المتطلبات

```
Flask==2.3.3
Flask-CORS==4.0.0
requests==2.31.0
python-dotenv==1.0.0
gunicorn==21.2.0
```

## 🔧 التثبيت والتشغيل المحلي

### 1. تثبيت المتطلبات

```bash
pip install -r requirements.txt
```

### 2. إعداد متغيرات البيئة

قم بإنشاء ملف `.env` وأضف مفتاح API الخاص بك:

```env
RAPIDAPI_KEY=your_rapidapi_key_here
FLASK_DEBUG=False
PORT=5000
```

### 3. تشغيل الخادم

```bash
python app_booking.py
```

الخادم سيعمل على: `http://localhost:5000`

## 📡 API Endpoints

### 1. الصفحة الرئيسية
```
GET /
```

### 2. فحص الصحة
```
GET /api/health
```

### 3. البحث عن المواقع
```
GET /api/locations/search?query=دبي
```

**المعاملات:**
- `query` (مطلوب): نص البحث

**مثال على الاستجابة:**
```json
{
  "success": true,
  "data": [
    {
      "dest_id": "-782831",
      "dest_type": "city",
      "label": "دبي، الإمارات العربية المتحدة"
    }
  ]
}
```

### 4. البحث عن الفنادق
```
POST /api/hotels/search
Content-Type: application/json

{
  "dest_id": "-782831",
  "checkin": "2025-12-20",
  "checkout": "2025-12-22",
  "adults": 2,
  "rooms": 1,
  "currency": "SAR"
}
```

**المعاملات المطلوبة:**
- `dest_id`: معرف الوجهة
- `checkin`: تاريخ الوصول (YYYY-MM-DD)
- `checkout`: تاريخ المغادرة (YYYY-MM-DD)

**المعاملات الاختيارية:**
- `adults`: عدد البالغين (افتراضي: 2)
- `rooms`: عدد الغرف (افتراضي: 1)
- `currency`: العملة (افتراضي: SAR)

**مثال على الاستجابة:**
```json
{
  "success": true,
  "data": [
    {
      "id": "12345",
      "name": "برج العرب جميرا",
      "address": "شارع جميرا، دبي",
      "rating": 5,
      "review_score": 9.4,
      "review_count": 3250,
      "price": 4500,
      "currency": "SAR",
      "image": "https://...",
      "latitude": 25.1412,
      "longitude": 55.1852
    }
  ],
  "total_results": 150
}
```

### 5. تفاصيل الفندق
```
GET /api/hotels/details?hotel_id=12345&checkin=2025-12-20&checkout=2025-12-22&adults=2&rooms=1&currency=SAR
```

**المعاملات المطلوبة:**
- `hotel_id`: معرف الفندق
- `checkin`: تاريخ الوصول
- `checkout`: تاريخ المغادرة

**مثال على الاستجابة:**
```json
{
  "success": true,
  "data": {
    "id": "12345",
    "name": "برج العرب جميرا",
    "description": "فندق فاخر...",
    "images": [...],
    "facilities": [...],
    "rooms": [...],
    "policies": {...}
  }
}
```

### 6. مراجعات الفندق
```
GET /api/hotels/reviews?hotel_id=12345&languagecode=ar
```

**المعاملات:**
- `hotel_id` (مطلوب): معرف الفندق
- `languagecode` (اختياري): كود اللغة (افتراضي: ar)

## 🌐 النشر على Render.com

### الخطوات:

1. **إنشاء حساب على Render**
   - اذهب إلى https://render.com
   - سجل حساب مجاني

2. **إنشاء Web Service جديد**
   - اضغط "New +" → "Web Service"
   - اربط مستودع GitHub الخاص بك
   - اختر مجلد `backend`

3. **إعدادات النشر**
   - **Name**: `paygo-booking-api`
   - **Environment**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn app_booking:app`
   - **Plan**: `Free`

4. **إضافة متغيرات البيئة**
   - اذهب إلى "Environment"
   - أضف:
     - `RAPIDAPI_KEY`: مفتاح API الخاص بك
     - `FLASK_DEBUG`: `False`

5. **النشر**
   - اضغط "Create Web Service"
   - انتظر حتى يكتمل النشر (2-3 دقائق)
   - ستحصل على رابط مثل: `https://paygo-booking-api.onrender.com`

## 🔒 الأمان

⚠️ **مهم جداً:**
- لا تضع مفتاح API في الكود مباشرة
- استخدم متغيرات البيئة دائماً
- لا ترفع ملف `.env` إلى GitHub
- أضف `.env` إلى `.gitignore`

## 📝 ملاحظات

- الخطة المجانية على Render تسمح بـ 750 ساعة شهرياً
- الخادم قد ينام بعد 15 دقيقة من عدم النشاط (في الخطة المجانية)
- أول طلب بعد النوم قد يستغرق 30-60 ثانية

## 🆘 المساعدة

إذا واجهت مشاكل:
1. تحقق من سجلات الخادم (Logs)
2. تأكد من صحة مفتاح API
3. تأكد من تنسيق التواريخ (YYYY-MM-DD)
4. تحقق من أن الخادم يعمل: `GET /api/health`

## 📄 الترخيص

مفتوح المصدر - استخدم بحرية!
